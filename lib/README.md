# Library jars

This folder contains the runtime dependencies of the HW7 web app.

You don't need to do anything with these files. The `Makefile` puts `lib/*` on
the classpath when compiling and running, and VS Code's Java extension picks
them up automatically.
